var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Parameter-Object-Generation","page":"API Reference","text":"","title":"Parameter Object Generation"},{"category":"section","location":"api/#Parameter-Map-Generation","page":"API Reference","text":"","title":"Parameter Map Generation"},{"category":"section","location":"api/#Caching-and-Updates","page":"API Reference","text":"","title":"Caching and Updates"},{"category":"section","location":"api/#Serialization","page":"API Reference","text":"","title":"Serialization"},{"category":"section","location":"api/#Type-Conversions","page":"API Reference","text":"Base.Dict(::MTKParams)\nBase.setproperty!(::MTKParams, ::Dict)\nBase.copy(::MTKParams)","title":"Type Conversions"},{"category":"section","location":"api/#Internals","page":"API Reference","text":"Use this functions to access the internal properties of a MTKParams ","title":"Internals"},{"category":"type","location":"api/#ModelingToolkitParameters.MTKParams","page":"API Reference","text":"MTKParams(Model::Function; kwargs...)\nMTKParams(sys::System;     kwargs...)\n\nA mutable, hierarchical parameter container for a ModelingToolkit System. Each field mirrors a parameter or sub-system of the underlying model and can be read or mutated with normal getproperty/setproperty! syntax (e.g. pars.resistor.R = 2). Bounds attached to parameters via @parameters X, [bounds=(lo, hi)] are enforced on assignment.\n\nThe system must NOT be structurally simplified — construct it with @named (@mtkcompile/@mtkbuild will throw). Initial values come from ModelingToolkit.initial_conditions(sys). Any keyword arguments are applied as parameter overrides after construction.\n\nUse pmap (or model => pars) to convert a MTKParams into the parameter map expected by ODEProblem/SciMLBase.remake, and cache + update! for fast in-place updates.\n\nExamples\n\npars = MTKParams(RCModel)\npars.resistor.R = 2.0\n\npars = MTKParams(ConstantVoltage; V = 20)\n\n\n\n\n\n","title":"ModelingToolkitParameters.MTKParams"},{"category":"macro","location":"api/#ModelingToolkitParameters.@mtkparams","page":"API Reference","text":"@mtkparams name = Model(; sub = ChildComponent(p = 1), kw = value, ...)\n@mtkparams const name = Model(; ...)\n@mtkparams Model(; ...)                                   # bare-call form\n\nConvenience macro that rewrites Model(...) into MTKParams(Model; ...), recursively transforming nested component constructor calls into nested MTKParams calls. Wrapping an assignment lets the catalog name appear in front of the macro so the declaration reads top-to-bottom; const is also supported. The bare-call form (name = @mtkparams Model(...)) still works.\n\nExample\n\n@mtkparams seat_pars = MassSpringDamper(\n    body   = Mass(m = 100),\n    spring = Spring(k = 1000),\n    damper = Damper(d = 1),\n)\n\nexpands (roughly) to\n\nseat_pars = MTKParams(MassSpringDamper;\n    body   = MTKParams(Mass;   m = 100),\n    spring = MTKParams(Spring; k = 1000),\n    damper = MTKParams(Damper; d = 1),\n)\n\n\n\n\n\n","title":"ModelingToolkitParameters.@mtkparams"},{"category":"function","location":"api/#ModelingToolkitParameters.pmap","page":"API Reference","text":"pmap(model::System, pars::MTKParams) -> Dict\n\nBuild a Dict{symbolic_parameter, value} keyed by the symbolic parameters of model. This is the form accepted by update! and the cache-aware SciMLBase.remake(prob, setters, param_dict) method.\n\nFor the flat Vector{Pair} form expected by ODEProblem and the standard SciMLBase.remake(prob; p = ...), write model => pars instead.\n\n\n\n\n\n","title":"ModelingToolkitParameters.pmap"},{"category":"method","location":"api/#Core.Pair-Tuple{System, MTKParams}","page":"API Reference","text":"Pair(model::System, pars::MTKParams) -> Vector{Pair}\n\nFlatten pars against model into a Vector{Pair} of symbolic_parameter => value entries (recursively walking sub-systems). This is the form accepted by ODEProblem and SciMLBase.remake(prob; p = ...). Equivalent to writing model => pars.\n\nFields of pars that don't have a matching property on model produce a warning and are skipped.\n\n\n\n\n\n","title":"Core.Pair"},{"category":"function","location":"api/#ModelingToolkitParameters.cache","page":"API Reference","text":"cache(model::System, x::MTKParams; parent = model) -> Vector{ParameterHookWrapper}\n\nPre-build a vector of SymbolicIndexingInterface setter functions, one per parameter field reachable from x (recursing into sub-systems). Pass the result, together with a parameter map from pmap, to update! or SciMLBase.remake to mutate an ODEProblem without rebuilding the setters on each call.\n\nparent is the top-level system used when constructing each setp setter; it only differs from model when cache recurses into a sub-system.\n\n\n\n\n\n","title":"ModelingToolkitParameters.cache"},{"category":"function","location":"api/#ModelingToolkitParameters.update!","page":"API Reference","text":"update!(prob::ODEProblem,\n        setters::Vector{ParameterHookWrapper},\n        param_dict::Dict) -> prob\n\nMutate prob in place by applying every setter in setters whose target parameter appears in param_dict. setters is produced by cache and param_dict by pmap. Entries with missing values are skipped (the underlying setters do not accept missing). Returns prob.\n\n\n\n\n\n","title":"ModelingToolkitParameters.update!"},{"category":"function","location":"api/#ModelingToolkitParameters.save_parameters","page":"API Reference","text":"save_parameters(x::MTKParams, filepath::String)\n\nWrite x to filepath as a hierarchical TOML file. missing values are stored as the string \"missing\" so they round-trip through load_parameters.\n\n\n\n\n\n","title":"ModelingToolkitParameters.save_parameters"},{"category":"function","location":"api/#ModelingToolkitParameters.load_parameters","page":"API Reference","text":"load_parameters(filepath::String, model::Function) -> MTKParams\n\nConstruct a fresh MTKParams(model) and apply the values stored in the TOML file at filepath (typically written by save_parameters).\n\n\n\n\n\nload_parameters(filepath::String, sys::System) -> MTKParams\n\nConstruct a fresh MTKParams(sys) and apply the values stored in the TOML file at filepath (typically written by save_parameters).\n\n\n\n\n\nload_parameters(filepath::String, x::MTKParams) -> MTKParams\n\nApply the values stored in the TOML file at filepath  (typically written by save_parameters) to the x::MTKParams  parameter object.\n\n\n\n\n\n","title":"ModelingToolkitParameters.load_parameters"},{"category":"function","location":"api/#ModelingToolkitParameters.parameters_to_string","page":"API Reference","text":"parameters_to_string(x::MTKParams) -> String\n\nReturn the TOML representation of x as a String. Same format as save_parameters writes, but without touching the filesystem.\n\n\n\n\n\n","title":"ModelingToolkitParameters.parameters_to_string"},{"category":"function","location":"api/#ModelingToolkitParameters.string_to_parameters","page":"API Reference","text":"string_to_parameters(contents::String, x::MTKParams) -> MTKParams\n\nApply parameter values parsed from the TOML string contents to the existing MTKParams instance x, mutating it in place. Returns x.\n\n\n\n\n\n","title":"ModelingToolkitParameters.string_to_parameters"},{"category":"function","location":"api/#ModelingToolkitParameters.get_parent","page":"API Reference","text":"get_parent(p::MTKParams) -> System\n\nReturn the underlying (un-simplified) System that backs p. Use this instead of p.parent, since getproperty on a MTKParams looks up parameters by name.\n\n\n\n\n\n","title":"ModelingToolkitParameters.get_parent"},{"category":"function","location":"api/#ModelingToolkitParameters.get_defs","page":"API Reference","text":"get_defs(p::MTKParams) -> Dict\n\nReturn the internal symbolic_parameter => value dictionary holding the current overrides for p. Mutating the returned dict mutates p.\n\n\n\n\n\n","title":"ModelingToolkitParameters.get_defs"},{"category":"section","location":"#ModelingToolkitParameters-Introduction-and-Motivation","page":"Home","text":"Currently the standard way to build parameters up in ModelingToolkit models is with the following pattern of keyword arguments with default values.  \n\nusing ModelingToolkit\nusing ModelingToolkit: t_nounits as t, D_nounits as D\nusing OrdinaryDiffEq\n\n@component function Motor(; name, k = 0.1, r = 0.01, l = 1e-3)\n  pars = @parameters begin\n    k = k\n    r = r\n    l = l\n  end\n  vars = @variables begin\n    v(t)\n    dphi(t) = 0\n    i(t) = 0\n    di(t)\n  end\n  eqs = Equation[\n    D(i) ~ di\n    v ~ i * r + l * di + dphi * k\n    D(dphi) ~ k * i\n    v ~ sin(t)\n  ]\n\n  return ODESystem(eqs, t, vars, pars; name)\nend\n\nThis patern heavily relies on the defaults mechanism of the model to actually set the parameters of the model.  Let say we want k, r, l to be 1, 2, 3.  One way we can do this is\n\n@mtkcompile sys = Motor(k=1, r=2, l=3)\n\nThis is simple enough, but what if we want another instance with k, r, l to be 4, 5, 6?  Should we fully rebuild the model? No, this is inefficient.  Instead it's better to set parameters at the ODEProblem level with remake to reuse the already structurally simplified system...\n\n@mtkcompile sys = Motor() # build system once\nprob = ODEProblem(sys, [], (0,1)) # build problem once\n\nprob1 = remake(prob; p = [sys.k => 1, sys.r => 2, sys.l => 3]) # reuse prob, update parameters\nprob2 = remake(prob; p = [sys.k => 4, sys.r => 5, sys.l => 6]) # reuse prob, update parameters again\n\nNote, now we are not using the keyword arguments of the model constructor now.  Instead we are building the parameter map from scratch.  Therefore, the cons of the keyword model construction pattern are:\n\nmakes the model interface more complicated.  As the list of parameters grows, the keyword list becomes un-manageable\nit's essentially useless if more than one model instance is needed\nbuilding a parameter map for remake still needs to be done from scratch.  (We could get the defaults dictionary of the model, but we still need to index into each parameter with sys.k, sys.r, etc.)\n\nThe additional problems with this pattern are:\n\nthere are no ways to enforce parameter settings rules with useful error messages, for example ensuring postive numbers \nparameter maps are not easy to work with since they are flat\nparameter maps are not printed with heirarcy and are therefore not easily saved/retrieved to/from file using TOML or JSON","title":"ModelingToolkitParameters - Introduction and Motivation"},{"category":"section","location":"#A-Better-Way","page":"Home","text":"ModelingToolkitParameters.jl provides a type MTKParams that creates a parameter object that is much easier to work with.  The Motor component can now be defined as follows with no keyword arguments.  Then we can generate a MTKParams object to hold parameter values.\n\nusing ModelingToolkitParameters\n\n@component function Motor(; name)\n  pars = @parameters begin\n    k = 0.1\n    r = 0.01\n    l = 1e-3\n  end\n  vars = @variables begin\n    v(t)\n    dphi(t) = 0\n    i(t) = 0\n    di(t)\n  end\n  eqs = Equation[\n    D(i) ~ di\n    v ~ i * r + l * di + dphi * k\n    D(dphi) ~ k * i\n    v ~ sin(t)\n  ]\n\n  return ODESystem(eqs, t, vars, pars; name)\nend\n\n@mtkparams motor_pars = Motor()\n\nWhich gives...\n\nMotor\n├─ k: 0.1\n├─ r: 0.01\n└─ l: 0.001\n\nnote: keyword args\nNote we can also enter keyword arguments here to override defaults.  Additionally, as will be shown in the next section, this can be done for child components as well.julia> @mtkparams motor_pars = Motor(k=12, r=23, l=34)\nMotor\n├─ k: 12\n├─ r: 23\n└─ l: 34\n\nA parameter map used for building ODEProblem can be generated from pmap(sys::ModelingToolkit.System, p::MotorParams), for example\n\n@mtkcompile sys = Motor()\n\nmotor_pars.k=1\nmotor_pars.r=2\nmotor_pars.l=3\n\nps = pmap(sys, motor_pars)\n\nGives...\n\n3-element Vector{Pair}:\n k => 1.0\n r => 2.0\n l => 3.0\n\nNow we can easily modify parameters using the Julia parameter object motor_pars.  Like\n\nprob = ODEProblem(sys, ps, (0, 0.1))\n\nmotor_pars2 = copy(motor_pars)\nmotor_pars2.k = 4.0\n\nprob2 = remake(prob; p = pmap(sys, motor_pars2))\n\nNow that our parameters are given by an object, we have many additional benefits:\n\nbetter display of parameters in tree form\nparameter bounds are applied\nsave/load from file easily\ncopy, mutate, and remake (including with a fast cache method shown below)","title":"A Better Way"},{"category":"section","location":"#Model-Heirarchy-and-Catalogs","page":"Home","text":"Let's explore a heirarchal model and how we can build and apply a catalog.  We will build a MassSpringDamper component that has the child systems Mass, Spring, and Damper. The Active Suspension model seen in the examples defines the simple mass, spring, damper components all with no defaults and no keyword constructors.\n\nNow, we can build a composite component MassSpringDamper\n\n@component function MassSpringDamper(;name)\n\n    systems = @named begin\n        damper = Damper()\n        body = Mass()\n        spring = Spring()\n        port_m = MechanicalPort()\n        port_sd = MechanicalPort()        \n    end\n\n    eqs = [       \n        connect(damper.flange_a, spring.flange_a, body.flange, port_m)\n        connect(port_sd, spring.flange_b, damper.flange_b)\n    ]\n\n    return System(eqs, t, [], []; systems, name)\nend\n\nThis model will require several MassSpringDampers representing the wheels, car, and seat.  We can build a Catalog of these components easily using the @mtkparams macro like\n\n@mtkparams seat_pars  = MassSpringDamper(body=Mass(m=100),  spring=Spring(k=1000), damper=Damper(d=1))\n@mtkparams car_pars   = MassSpringDamper(body=Mass(m=1000), spring=Spring(k=1e4),  damper=Damper(d=10))\n@mtkparams wheel_pars = MassSpringDamper(body=Mass(m=25),   spring=Spring(k=1e2),  damper=Damper(d=1e4))\n\nNow when we build the top level model we can set the component initial_conditions to the catalog items as follows...\n\n@component function Model(; name)\n\n    systems = @named begin\n        seat = MassSpringDamper()\n        car_and_suspension = MassSpringDamper()\n        wheel = MassSpringDamper()\n        road_data = Road()\n        road = Position()\n        force = Force()\n        pid = Controller()\n        err = Subtract() \n        set_point = Constant()\n        seat_pos = PositionSensor()\n        flip = Gain(k=-1)\n    end\n\n    initial_conditions = [\n        (seat => seat_pars)...\n        (car_and_suspension => car_pars)...\n        (wheel => wheel_pars)...\n    ]\n\n    . . .\n\n    return System(eqs, t, [], pars; systems, name, initialization_eqs, initial_conditions)\nend\n\nNow we can build a parameter object of Model as follows and see that the catalog of MassSpringDamper parameters was implemented...\n\njulia> sys_pars = MTKParams(ActiveSuspensionModel.Model)\nModel\n├─ g: -9.807\n├─ seat\n│  ├─ damper\n│  │  └─ d: 1.0\n│  ├─ body\n│  │  └─ m: 100.0\n│  └─ spring\n│     ├─ k: 1000.0\n│     └─ initial_stretch: missing\n├─ car_and_suspension\n│  ├─ damper\n│  │  └─ d: 10.0\n│  ├─ body\n│  │  └─ m: 1000.0\n│  └─ spring\n│     ├─ k: 10000.0\n│     └─ initial_stretch: missing\n├─ wheel\n│  ├─ damper\n│  │  └─ d: 10000.0\n│  ├─ body\n│  │  └─ m: 25.0\n│  └─ spring\n│     ├─ k: 100.0\n│     └─ initial_stretch: missing\n├─ road_data\n│  ├─ bump: 0.2\n│  ├─ freq: 0.5\n│  ├─ offset: 1.0\n│  └─ loop: 10\n├─ pid\n│  ├─ kp: 1.0\n│  ├─ ki: 0.2\n│  └─ kd: 20.0\n├─ err\n│  ├─ k1: 1.0\n│  └─ k2: -1\n├─ set_point\n│  └─ k: 0\n└─ flip\n   └─ k: -1","title":"Model Heirarchy and Catalogs"},{"category":"section","location":"#Speed","page":"Home","text":"As mentioned previously, using the keyword default patern for model parameter setting is not a good way to build several model variations, as this requires fully compiling/simplifying the model from scratch each time.  A better way was shown with ModelingToolkitParameters.jl using remake and proving an updated parameter map.  However, this way is still not the fastest.  The most efficient approach is to use SymbolicIndexingInterface.jl.  ModelingToolkitParameters.jl provides a cache function that implements the SymbolicIndexingInterface.jl utility to provide a more efficient use of remake.  The example below demonstrates this comparison.\n\nusing ModelingToolkit\nusing ModelingToolkitParameters\nusing ActiveSuspensionModel\nusing SciMLBase\nusing BenchmarkTools\n\n@mtkcompile model = ActiveSuspensionModel.Model()\n@mtkparams model_pars = ActiveSuspensionModel.Model()\nprob = ODEProblem(model, pmap(model, model_pars), (0, 10))\n\n# Slow Option\nmodel_pars.seat.body.m = 200                                                # change parameters\nprob2 = remake(prob; p = pmap(model, model_pars))                           # remake ODEProblem\ntime_slow = @belapsed prob2 = remake($prob; p = pmap($model, $model_pars))  # remake ODEProblem (timed)\n\n# Fast Option\nmodel_setters = cache(model, model_pars);# build cache (one time only)\n\nmodel_pars.seat.body.m = 300                                                            # change parameters\nprob3 = remake(prob, model_setters, pmap(model, model_pars))                            # fast remake ODEProblem\ntime_fast = @belapsed prob3 = remake($prob, $model_setters, pmap($model, $model_pars))  # fast remake ODEProblem (timed)\n\n@show time_slow time_fast # hide\nnothing # hide","title":"Speed"},{"category":"section","location":"#Saving-and-Loading-Parameters","page":"Home","text":"The advantage of storing parameters into structs is that they can now be easily saved and loaded from text file.  The code below will demonstrate how to use the TOML format to create parameter files.  The TOML library in Julia can easily write out code if the data is in a dictionary.  We can easily convert our parameter structs to dictionary.\n\nDict(model_pars)\n\nSaving with TOML format (which can easily print from a Dict) can be done using\n\nsave_parameters(model_pars, \"model_pars.toml\")\n\nThis will save out a file that looks like...\n\nusing TOML\nTOML.print(ModelingToolkitParameters.convert_value, Dict(model_pars))\n\nNote: `convertvalue` can be used to help with saving complex types.  See TOML documentation for more information._\n\nThen loading from file is done with load_parameters(filepath::String, T::Type) where T is the matching parameter struct type...\n\nmodel_pars = load_parameters(\"model_pars.toml\", ActiveSuspensionModel.Model) \n\nFrom text, this works like the following\n\n@mtkparams p = ActiveSuspensionModel.Road()\nsetproperty!(p, TOML.parse(\"\"\"\n                          bump = 0.3\n                          freq = 0.75\n                          offset = 3\n                          \"\"\"))\nDict(p)","title":"Saving and Loading Parameters"},{"category":"section","location":"#Structural-Parameters","page":"Home","text":"In some cases we might have a model that uses structural parameters, ones that change the system itself.  For example the model below has a structural parameter use_resistor which changes the model structurally, by either including or not including a resistor in the circuit.  \n\n@component function RCModel(use_resistor=true; name)\n    systems = @named begin\n        capacitor = Capacitor()\n        source = ConstantVoltage()\n        ground = Ground()\n    end\n\n    initial_conditions = [\n        (source => special)...\n    ]\n\n    if use_resistor\n        @named resistor = Resistor()\n        push!(systems, resistor)\n    end\n    \n    eqs = if use_resistor\n        [\n            connect(source.p, resistor.p)\n            connect(resistor.n, capacitor.p)\n            connect(capacitor.n, source.n)\n            connect(capacitor.n, ground.g)\n        ]\n    else\n        [\n            connect(source.p, capacitor.p)\n            connect(capacitor.n, source.n)\n            connect(capacitor.n, ground.g)\n        ]\n    end\n    return System(eqs, t, [], []; name, systems, initial_conditions)\nend\n\nFor a model like this, we can't simply use the form @mtkparams rcmodel_res_pars = RCModel() or rcmodel_res_pars = MTKParams(RCModel), because RCModel could have 2 different sets of parameters based on the build time structural parameter.  Instead, what we can do is build the parameter object from model instances, post build time, like...\n\ninclude(\"../../test/model.jl\") # hide\n@named rc_model_res = RCModel(true)\nrc_model_res_pars = MTKParams(rc_model_res)\n\nAnd then without the resistor\n\n@named rc_model_nor = RCModel(false)\nrc_model_nor_pars = MTKParams(rc_model_nor)\n\nnote: load_parameters\nThe same concept applies to load_parameters.  The 2nd argument can be either:a model constructor function\nan instantiated model System\na MTKParams parameter object\n\nwarning: avoid simplified system\nMTKParams accepts only a non-structurally simplified System (i.e. use @named and not @mtkcompile ).  This is necessary so the sub-systems information is available, as structurally simplified systems are flattened.  ","title":"Structural Parameters"}]
}
